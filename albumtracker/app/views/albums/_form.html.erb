<%= form_for(@album) do |f| %>
  <% if @album.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@album.errors.count, "error") %> prohibited this album from being saved:</h2>

      <ul>
      <% @album.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :albumtitle %><br>
    <%= f.text_field :albumtitle %>
  </div>
  <div class="field">
    <%= f.label :genre %><br>
    <%= f.text_field :genre %>
  </div>
  <div class="field">
    <%= f.label :yearreleased %><br>
    <%= f.number_field :yearreleased %>
  </div>
  <div class="field">
    <%= f.label :dateadded %><br>
    <%= f.date_select :dateadded %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
